{
  "name": "llm_inference_demo",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "main": "index.js",
  "scripts": {
    "dev": "rimraf dist && cp -r static dist && esbuild --bundle --target=es2020 --watch --outfile=dist/bundle.js --serve --servedir=dist --sourcemap src/index.ts",
    "build": "rimraf dist && cp -rL static dist && esbuild --bundle --target=es2020 --outfile=dist/bundle.js --sourcemap src/index.ts",
    "lint": "tsc && eslint"
  },
  "author": "",
  "description": "",
  "dependencies": {
    "@huggingface/hub": "^2.6.5",
    "@mediapipe/tasks-genai": "^0.10.26",
    "lit": "^3.3.0",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/deep-equal": "^1.0.4",
    "deep-equal": "^2.2.3",
    "esbuild": "^0.25.0",
    "eslint": "^9.36.0",
    "immer": "^10.1.1",
    "rxjs": "^7.8.2",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.44.0"
  }
}
